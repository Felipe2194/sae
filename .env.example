# Copiar a .env.local y completar los valores.

# Conexión a PostgreSQL (docker compose up -d levanta la base en localhost:5432)
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/sae

# Auth.js — generar con: openssl rand -base64 32
AUTH_SECRET=
AUTH_URL=http://localhost:3000
